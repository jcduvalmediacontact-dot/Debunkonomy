# Rapports d'audit contradictoire et de recherche documentaire.
# Décision de l'auteur du 2026-09-04 : ils restent HORS DÉPÔT.
# Le dépôt est public et sert debunkonomy.org ; ces rapports listent les erreurs
# corrigées et les objections non résolues du corpus avant arbitrage.
# Archive de référence : Drive, Corpus GPT / SAUVEGARDE CORPUS / audits-et-recherches
# Voir protocoles/passe-2.md § 1.
audits-et-recherches/
protocoles/audits/
protocoles/audits-et-recherches/
*-audit.md
*-deep-research.md
# Exception : le protocole generique est un MODELE a coller, pas un rapport.
# CLAUDE.md le declare faisant autorite ; le motif d exclusion ne le vise pas.
!protocoles/sourcage-deep-research.md

# COMPLETEMENT DU 2026-09-15 : les motifs ci-dessus n'attrapaient AUCUN des
# fichiers qu'ils visent. Tous finissent par un identifiant de chapitre —
# dossier-audit-L1-C03.md, rapport-audit-L1-C15-tiers.md — et non par
# « -audit.md ». Quarante-quatre fichiers seraient entres dans un depot public
# au premier « git add -A ». Les sept familles sont nommees explicitement.
protocoles/rapport-audit-*.md
protocoles/dossier-audit-*.md
protocoles/paquet-adverse-*.md
protocoles/audit-factuel-*.md
protocoles/verification-audit-*.md
protocoles/traitement-objections-*.md
# Exception : le protocole generique d'audit contradictoire est un MODELE a
# coller, pas un rapport, et CLAUDE.md le declare faisant autorite.
!protocoles/audit-contradictoire.md


# COMPLETEMENT DU 2026-09-16, decision de l'auteur : la memoire de travail va
# dans un DEPOT PRIVE SEPARE, jamais ici.
# coordination/ tient le journal des relais (440 Ko, 71 erreurs, defauts et
# objections non resolues recenses), l'index des decisions, la file de travail
# et les listes de courses. C'est exactement la matiere que le motif du
# 2026-09-04 garde hors d'un depot public, et elle n'etait attrapee par aucun
# des motifs ci-dessus.
# Ce dossier porte son propre depot git, pousse vers un remote prive distinct.
coordination/

# SORTIE DU GENERATEUR DU § 13, ajoutee le 2026-09-16.
# corpus/generer.py reconstruit ce dossier entier a chaque execution : il n'a
# pas a etre versionne, et la convention interdit d'y toucher a la main.
# Quand l'auteur decidera de publier, la sortie ira dans l'arbre du site, ou
# elle devra etre suivie ; ce motif ne vise que le dossier de travail.
corpus/genere/

# Construction locale du site et du corpus. Cette sortie est reconstruite par
# outils/construire_site.py ; elle n'est jamais une source à versionner.
build/
.build-en-cours/
.build-env/

# Sauvegardes locales et archives
Sauvegarde*/
*.zip

# verrous LibreOffice
.~lock.*#

# Caches Python des pieces executables de modeles/
__pycache__/
*.pyc
